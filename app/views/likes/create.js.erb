$('#like-button').html("<%= j render 'like_button', :object => @photo %>");
$('#like-count').html("<%= @likes.count %>");
$('#like-user').html("<% @likes.each do |like| %> <%= j image_tag(like.user.picture, :width => '40') %> <% end %>");