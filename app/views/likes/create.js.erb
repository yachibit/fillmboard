$("<%= "#like_button#{@photo.id}" %>").html("<%= j render 'like_button', :photo_id => @photo.id %>");
$("<%= "#like_count#{@photo.id}" %>").html("<%= @likes.count %>");
$('#like-user').html("<% @likes.each do |like| %> <%= j image_tag(like.user.picture, :width => '40') %> <% end %>");