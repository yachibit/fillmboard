<% current_user = User.first %>
<% if @photo %>
<% photo = @photo %>
<% end %>

<%= form_for current_user.comments.build(:photo_id => photo.id), :remote => true do |f| %>
		<%= f.hidden_field :photo_id %>
		<%= f.hidden_field :user_id %>
		<div class="field">
      <%#= f.label :body %><%#br /%>
      <%= f.text_area :body, :size => "20×10" %>
    </div>
		<%= f.submit "Comment" %>
<% end %>